#ifndef ASSETLOADER_H
#define ASSETLOADER_H

#include "GameData.h"
#include <string>

using namespace std;

// Function to load a game event from a file
GameEvent LoadEvent(string eventId);

#endif // ASSETLOADER_H
